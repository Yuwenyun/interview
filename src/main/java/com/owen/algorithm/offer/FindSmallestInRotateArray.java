package com.owen.algorithm.offer;

/**
 * put sorted array(asc)'s first n numbers to the tail, the new array is called
 * rotated array.
 * find the smallest element.
 *
 * {3, 4, 5, 1, 2} -> 1
 */
public class FindSmallestInRotateArray {

    public static void main(String[] args){

    }

    private int getMin(int[] array, int start, int end)
    {
        if(array == null || array.length == 0){
            return 0;
        }

        int x = start, y = end;
        return 0;
    }
}
